"""Shared components used across algorithm implementations.

Modules:
    checkpoint.py  - Checkpoint data class, RNG capture/restore, config snapshot builder
    functional.py  - Neural network building blocks (_make_fc, _make_conv)
"""
